

max = (x, y, ?cmp = { ? < ? }) -> {
    if (cmp(x, y))
        return y
    return x
}
%% (x, y, *v, ?cmp = { ? < ? }) -> {
    return reduce([x, y] .. v, { max(?, ?, $cmp) })s
}
%% (v, ?cmp = { ? < ? }) -> {
    return max(*v, cmp=cmp)
}